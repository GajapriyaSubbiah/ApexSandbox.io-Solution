public String formatName(String firstName, String lastName) {
    //code here
    if (String.IsBlank(firstName) || String.IsBlank(lastName)){
        String formatName =lastName + ', ' + firstName;
        return formatName;
    }else{
        String formatName =lastName + ', ' + firstName;
        return formatName;
    }
}