public String nameFromEmail(String email) {
    String substr = email.substringBefore('@');
    system.debug(substr);
    String firstname = capitalize(substr.substringBefore('.'));
    String lastname = capitalize(substr.substringAfter('.'));
    String fullName = firstname + lastname;
    
    return fullName;
}