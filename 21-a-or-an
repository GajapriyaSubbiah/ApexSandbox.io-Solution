public String aOrAn(String word) {
    //code here
    String word = word.trim();
    if(word.startsWith('a') || word.startsWith('e')){
        return word = an + word;
    }else{
        return word = a + word;
    }
}