public String aOrAn(String word) {
    //code here
    String result = word.trim();
    if(result.startsWith('a') || result.startsWith('e')){
        return result = an + word;
    }else{
        return result = a + word;
    }
}